#include "main.h"
/**
* mul - multiplies two integers
* @a: first parameter
* @b: second parameter
*
* Decsription: function that multiplies two integers.
* Return: Always(0) success
*/
int mul(int a, int b);
{
	int mull;

	mull = a * b;

	return (mull);
}
