#include <stdio.h>

/**
* main - Entry point
*
* Description - this programs checkes wheather a number
* is positive or negative
*
* Return - Always 0 (success)
*/

int main(void)
{
	/* my statement */
	for (char i = 'a'; i <= 'z'; i++)
	{
		/* code */
		putchar(i);
	}
	putchar('\n');

	return(0);
}
